const express = require("express");
const AuthController = requrie("../controllers/auth.controller");

const router = express.Router();
const authController = new AuthController();

//

module.exports = router;
